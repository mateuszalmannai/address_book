// prints the process.arguments array
// The first two are the name of the command (node) and the path to the JavaScript
// file being run
console.log(process.argv);
/* Whenever we provide arguments to a node.js app they will shop up in
 * process.argv(ector) starting with index two
 */
